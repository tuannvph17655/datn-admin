export interface ProductOrder {
    id ?: string,
    name ?: string,
    color ?: string,
    size ?: string,  
    image ?: string,
    material ?: string,
    category ?: string,
    priceFmt ? : string,
    total ? : string,
    qty ?: number,
    totalFmt ?: string
}