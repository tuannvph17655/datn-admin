export interface ProductOrder {
    id ?: string,
    name ?: string,
    color ?: string,
    size ?: string,
    image ?: string,
    material ?: string,
    price ?: string,
    qty ?: number,
    category ?: string,
    total ? : string,
}
