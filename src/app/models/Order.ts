export interface Order {
  orderId ?: string,
  orderCode ?: string,
  status ?: string,
  createDate ?: Date,
  address ?: string,
  totalPrice ?: string,
  payed ?: boolean
  statusValue ?: string
}

