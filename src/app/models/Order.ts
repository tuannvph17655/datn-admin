export interface Order {
  orderId ?: string,
  orderCode ?: string,
  createDate ?: Date,
  address ?: string,
  totalPrice ?: string,
  payed ?: boolean
  status ?: string,
  statusValue ?: string
}

