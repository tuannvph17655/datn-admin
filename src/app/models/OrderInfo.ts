export interface OrderInfo {
    nameOfRecipient ?: string,
    phoneNumber ?: string,
    note ?: string,
    shipAddress ?: string,
    paymentMethod ?: string,
    payed ?: boolean,
    statusOrder ?: string,
    createDate ?: Date,
    statusOrderValue ? : string
}

